export default class Template {
    constructor(prefix, scope, body, description) {
        this.prefix = prefix;
        this.scope = scope;
        this.body = body;
        this.description = description;
    }
}
