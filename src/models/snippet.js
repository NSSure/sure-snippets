export default class Snippet {
    constructor(name, prefix, content) {
        this.id = '';
        this.name = name;
        this.prefix = prefix;
        this.content = content;
        this.scope = 'javascript';
    }
}
